const start = time();
func fib(n)
{
    if(n <= 0)
    {
        ret 0;
    }
    if(n == 1)
    {
        ret 1;
    }
    else
    {
        ret fib(n - 1) + fib(n - 2);
    }
}

print("fib(6): {fib(6)}");
const end = time();
print("Start time: {start}, end time: {end}\nExecuted in {(end - start) / 1000000000.0} seconds");
